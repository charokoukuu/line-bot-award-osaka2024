import { Team, TeamInfo } from "../types/user.type";

export const SetTeamInfo = async (teamInfo: TeamInfo) => {
  return "hogehoge";
};
export const SetTeam = async (teamInfo: Team) => {
  return "hogehoge";
};
